// Generated by CIRCT firtool-1.137.0-1-gfcc42190a

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module PWMLEDAXI_VerificationLayer();
  `ifndef SYNTHESIS
    always @(posedge PWMLEDAXI.clock) begin
      if (~PWMLEDAXI.reset
          & ~(~PWMLEDAXI.enable_0 | PWMLEDAXI.counter_0 <= PWMLEDAXI.period_0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: Counter exceeded period\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
  always @(posedge PWMLEDAXI.clock) begin
    if (~PWMLEDAXI.reset)
      cover__cover: cover(PWMLEDAXI.io_led_0_1);
  end // always @(posedge)
endmodule

